{
  "name": "smirnoff-ice-railway",
  "version": "1.0.0",
  "description": "Root package for deployment",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "install": "npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
